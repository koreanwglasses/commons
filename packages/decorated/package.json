{
  "name": "@koreanwglasses/commons-decorated",
  "version": "0.0.0",
  "packageManager": "yarn@3.1.1",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "$npm_execpath run build",
    "clean": "rm -rf dist",
    "build": "$npm_execpath run clean && tsc"
  },
  "devDependencies": {
    "@koreanwglasses/commons-core": "workspace:^",
    "@types/node": "^16.11.12",
    "tsc": "^2.0.3",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@koreanwglasses/cascade": "0.5.4",
    "reflect-metadata": "^0.1.13"
  }
}
